{
  "name": "optmem-anim",
  "private": true,
  "description": "Renders the OptMem explainer: render.js is the film, video.js writes the frames.",
  "type": "commonjs",
  "dependencies": {
    "canvas": "^3.2.3"
  }
}
